# ================================================
# AI Lecture Summarizer — Python Dependencies
# Manipal University Jaipur, CSE PBL 2026
# ================================================
# Install all at once:
#   pip install -r requirements.txt
# ================================================

# ── Web Framework ──
flask==3.0.3
flask-cors==4.0.1

# ── OpenAI SDK (GPT + Whisper API) ──
openai>=1.30.0

# ── PDF text extraction ──
PyPDF2==3.0.1

# ── DOCX text extraction ──
python-docx==1.1.2

# ── Video/Audio processing ──
moviepy==1.0.3

# ── Audio handling (Whisper dependency) ──
soundfile==0.12.1
ffmpeg-python==0.2.0

# ── Utilities ──
python-dotenv==1.0.1
gunicorn==22.0.0

# ── NumPy (Whisper dependency) ──
numpy>=1.24.0,<2.0.0
